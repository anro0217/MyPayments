<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/app_icon_rounded"
        android:label="@string/mypayments"
        android:roundIcon="@drawable/app_icon_rounded"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyPayment"
        tools:targetApi="31">
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MyPayment.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.MyPayment.NoActionBar">
        </activity>

        <activity
            android:name=".activities.NotesActivity"
            android:exported="true"
            android:theme="@style/Theme.MyPayment.NoActionBar" >
        </activity>

        <activity
            android:name=".activities.NewNoteActivity"
            android:exported="true"
            android:theme="@style/Theme.MyPayment.NoActionBar" >
        </activity>

        <activity
            android:name=".activities.SettingsActivity"
            android:exported="true"
            android:theme="@style/Theme.MyPayment.NoActionBar">
        </activity>

    </application>
    <uses-permission android:name="android.permission.INTERNET"
        tools:ignore="ManifestOrder" />
</manifest>